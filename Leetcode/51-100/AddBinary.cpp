class Solution {
public:
    string addBinary(string a, string b) {
        
        int am = a.length();
        int bm = b.length();
        if(am > bm)string ret = a;
        else string.ret = b;
        
        int m = min(am,bm);
        int index = 0;
        int next = 0;
        while(index <= m){
            char c = a[am - index] + b[bm - index] + next;
            if(c = '0')
            else if(c )
            
            
            
            
            
        index ++;
        }
        
        
        
    }
};
